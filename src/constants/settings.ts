export const LANGUAGES: string[] = ['it', 'en'];
export const THEMES = {
    light: {
        palette: {}
    },
    dark: {}
};
export const ROUTES = {
    index: '/',
    edit: '/edit',
    preview: '/view/:sourcesIds'
};

export const PREVIEW_PATH_DELIMITER = '_';
